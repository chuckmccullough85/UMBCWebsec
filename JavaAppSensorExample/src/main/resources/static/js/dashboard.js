// Dashboard auto-refresh functionality
$(document).ready(function() {
    // This could be enhanced to use AJAX for partial updates
    // instead of full page refresh
    
    // Auto-refresh is handled by meta refresh tag in the HTML
});
